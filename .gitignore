# Secrets / local env
.env
**/.env
benchmarks/.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Benchmark caches / large downloads (keep slim samples if committed intentionally)
benchmarks/data/in22_conv_npi_sample.jsonl
benchmarks/__pycache__/

# Local IDE
.cursor/

# Datasets — keep seeds/manifests; ignore bulky generated runs
datasets/synthetic/student_raw/
datasets/synthetic/teacher_reviewed/
datasets/benchmarks/runs/
training/artifacts/it2_en_indic_1b/
training/artifacts/it2_indic_en_1b/


# Large harvested / scraped corpora (rebuild from scripts)
training/data/raw/

# Transient bench logs
benchmarks/results/*.log
benchmarks/results/*.err.log

# Node / mobile (root safety)
node_modules/
.expo/

# OS
.DS_Store
Thumbs.db
