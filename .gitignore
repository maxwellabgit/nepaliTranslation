# Secrets / local env
.env
**/.env
benchmarks/.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Benchmark caches / large downloads (keep slim samples if committed intentionally)
benchmarks/data/in22_conv_npi_sample.jsonl
benchmarks/__pycache__/

# Local IDE
.cursor/

# Fine-tuned / merged model weights (multi-GB; keep scripts + small data in git)
training/artifacts/

# Large harvested / scraped corpora (rebuild from scripts)
training/data/raw/

# Transient bench logs
benchmarks/results/*.log
benchmarks/results/*.err.log

# Node / mobile (root safety)
node_modules/
.expo/
coverage/
mobile/coverage/

# OS
.DS_Store
Thumbs.db
