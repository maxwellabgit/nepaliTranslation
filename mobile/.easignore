# Also applied when packing from mobile/ (belt and suspenders).
# Anchor native folders to the app root so modules/*/ios and modules/*/android
# stay in the archive and pass Expo Doctor.
node_modules/
.expo/
dist/
web-build/
/ios/
/android/
assets/models/
*.bin
*.gguf
*.onnx
*.mlmodel
*.mlpackage
.env*.local
*.log
